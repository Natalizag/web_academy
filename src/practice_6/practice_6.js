import './practice_6.scss';

/*const salaries = {
    John: 1000,
    Jack: 5000,
    test: '2000',
    Olya: 'jhjh1'
}

function calcTotal(obj) {
    let total = 0;
    for (let key in obj) {

        let value = obj[key];

        if (typeof value != 'number') {
            value = parseInt(value);
        }

        if (isNan(value) === false) {
            total += value;
        }

    }
    return total;
}

const total = calcTotal(salaries);
console.log(total);*/